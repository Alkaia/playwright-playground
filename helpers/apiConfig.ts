/**
 * API Configuration
 * Note: DemoBlaze doesn't expose a public API, so we use JSONPlaceholder
 * as a demonstration API for API testing patterns.
 * In a real project, this would point to our application's API.
 */
export const ApiConfig = {
  API_BASE_URL: 'https://jsonplaceholder.typicode.com',
} as const;

